# Proteus

A terminal UI agent for de novo protein design. See `docs/SPECS.md`.

## Build

    make build
    ./bin/proteus

## Status

v0.1 — "Hello, sequence": chat TUI, agent loop, `fold.esmfold`.
